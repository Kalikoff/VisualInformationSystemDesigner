<UserControl x:Class="VisualInformationSystemDesigner.CustomElements.Console.ConsoleView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:VisualInformationSystemDesigner.CustomElements.Console"
			 
			 mc:Ignorable="d" 
			 d:DesignHeight="600"
			 d:DesignWidth="600"
			 d:DataContext="{d:DesignInstance Type=local:ConsoleViewModel}">
	<Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBox HorizontalAlignment="Stretch" 
				 VerticalAlignment="Stretch"
                 Grid.Row="0"
				 TextWrapping="Wrap"
				 AcceptsReturn="True"
				 Background="#0c0c0c"
				 Foreground="#cccccc"
				 FontFamily="Consolas"
				 FontSize="16"
                 Text="{Binding Source}"/>
        
        
    </Grid>
</UserControl>