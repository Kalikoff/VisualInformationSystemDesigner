<Page x:Class="VisualInformationSystemDesigner.View.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VisualInformationSystemDesigner.View"
      xmlns:viewModel="clr-namespace:VisualInformationSystemDesigner.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestPageView">

    <Page.DataContext>
        <viewModel:HomeViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0"></Menu>

        <!--Рабочая область-->
        <Frame Grid.Row="1">
            <Frame.ContentTemplate>
                <DataTemplate>
                    <local:WorkspaceView/>
                </DataTemplate>
            </Frame.ContentTemplate>
        </Frame>
    </Grid>
</Page>