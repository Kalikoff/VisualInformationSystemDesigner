<Page x:Class="VisualInformationSystemDesigner.View.Home.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VisualInformationSystemDesigner.View.Home"
      xmlns:viewModel="clr-namespace:VisualInformationSystemDesigner.ViewModel.Home"
      xmlns:workspace="clr-namespace:VisualInformationSystemDesigner.View.Workspace"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestPageView">

    <Page.DataContext>
        <viewModel:HomeViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Файл"/>
        </Menu>

        <!--Рабочая область-->
        <Frame Grid.Row="1" Background="{StaticResource SecondaryColor}">
            <Frame.ContentTemplate>
                <DataTemplate>
                    <workspace:WorkspaceView/>
                </DataTemplate>
            </Frame.ContentTemplate>
        </Frame>
    </Grid>
</Page>